{
  "name": "alisonrafael.me",
  "version": "1.0.0",
  "description": "Monorepo for alisonrafael.me: resume site (web-client) + API (server)",
  "license": "ISC",
  "author": "Alison Rafael Marinho Gonçalves",
  "private": true,
  "workspaces": [
    "web-client",
    "server"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=web-client",
    "dev:api": "npm run start:dev --workspace=server",
    "build:web": "npm run build --workspace=web-client",
    "build:api": "npm run build --workspace=server",
    "build": "npm run build:web && npm run build:api"
  }
}
