// Placeholder — the real resume page (ported from src/pages/ResumePage.tsx)
// lands in Phase 2. This phase just proves the Next.js scaffold builds and
// serves. See docs/adr/0013-migrate-web-client-to-nextjs-app-router.md.
export default function Home() {
  return <div>OK</div>
}
